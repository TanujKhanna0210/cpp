#include<iostream>
using namespace std;

int main()
{
    int p,c,m;
    float avg;
    cin >> p;
    cin >> c;
    cin >> m;
    avg = (p+c+m)/3.0;
    cout << "Average Value : " << avg;
    return 0;
}
